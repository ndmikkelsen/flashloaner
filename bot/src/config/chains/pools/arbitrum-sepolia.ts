import type { PoolDefinition } from "../../types.js";

/**
 * Arbitrum Sepolia testnet pool definitions.
 *
 * Empty initially - testnet pools will be populated after deployment and discovery.
 * Testnet has fake/sparse liquidity, so pools are discovered during Phase 4 testing.
 */
export const ARBITRUM_SEPOLIA_POOLS: PoolDefinition[] = [];
