# Kamal 2 shared base config for flashloaner infrastructure
#
# Destinations:
#   kamal deploy -d cognee    Cognee API + PostgreSQL/pgvector
#   kamal deploy -d dolt      Dolt SQL server (Beads backend)
#
# This base config is not deployed directly — always use a destination.

service: flashloaner

image: flashloaner/base

servers:
  - 10.10.20.138

registry:
  server: harbor.compute.lan
  username: admin
  password:
    - KAMAL_REGISTRY_PASSWORD

ssh:
  user: compute
  keys:
    - ~/.ssh/z3r0Layer-main

builder:
  arch: amd64
