# Environment files (contain secrets)
.env
.env.local
.env.*.local
.env.server-backup
.env.arbitrum-sepolia
.env.arbitrum
.env.base-sepolia
.env.base

# ── Foundry ──
out/
cache/
# broadcast/ — Deployment logs are committed for auditability.
# Exclude sensitive run-latest symlinks but keep actual broadcast records.
broadcast/**/run-latest/

# ── Node / TypeScript ──
node_modules/
dist/
.turbo/
coverage/

# ── Python (scripts/tooling) ──
__pycache__/
*.pyc
.pytest_cache/
.coverage
*.egg-info/
.venv/

# ── Logs ──
*.log

# ── IDE ──
.vscode/
.idea/

# ── OS ──
.DS_Store
Thumbs.db

# ── DeFi Specific ──
# Fork cache (anvil/hardhat)
fork-cache/

# ── Claude Code local settings (user-specific) ──
.claude/settings.local.json

# ── Session context (local only) ──
STICKYNOTE.md

# ── Beads sync local state ──
.beads/.sync.lock
.beads/sync_base.jsonl
.beads/*.db
.beads/export/
.beads/.gitignore
.beads/README.md
.beads/interactions.jsonl
.beads/metadata.json

# ── Cognee local data ──
cognee_data/
